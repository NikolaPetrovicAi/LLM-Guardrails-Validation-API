services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .cache:/app/.cache
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=gpt-4-turbo
      - ENVIRONMENT=production
      - DEBUG=false
    restart: always
